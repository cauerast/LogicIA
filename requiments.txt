Hello there, :)

create the virtual environment
    py -m venv .venv

install the dependences in virtual environment:
    py -m pip install fastapi uvicorn python-dotenv python-multipart spacy
    py -m pip install -q -U google-genai

if needs:
    - spacy requiments

to run the project:
    py -m uvicorn main:app --reload